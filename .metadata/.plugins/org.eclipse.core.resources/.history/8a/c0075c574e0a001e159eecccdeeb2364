# Configure this Discovery Server
eureka:
  instance:
    hostname: localhost
  client: #Not a client
    register-with-eureka: false
    fetch-registry: false

# HTTP (Tomcat) port
server:
  port: 2345